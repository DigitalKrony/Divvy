/*!
 * Copyright (C) Design:Funedikly. All rights reserved.
 */

import { makeStyles } from "@griffel/react";

/**
 * Styles for the Modal slots
 */
export const useModalStyles = makeStyles({
  root: {},
});

export const useModalContentStyles = makeStyles({
  root: {},
});

export const useModalHeaderStyles = makeStyles({
  root: {},
});

export const useModalBodyStyles = makeStyles({
  root: {},
});

export const useModalFooterStyles = makeStyles({
  root: {},
});
