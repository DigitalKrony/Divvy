/*!
 * Copyright (C) Design:Funedikly. All rights reserved.
 */

import { makeStyles } from "@griffel/react";

/**
 * Styles for the NavBar slots
 */
export const useNavBarStyles = makeStyles({
  root: {},
});

export const useNavBarContentStyles = makeStyles({
  root: {},
});

export const useNavBarBrandStyles = makeStyles({
  root: {},
});

export const useNavBarItemStyles = makeStyles({
  root: {},
});

export const useNavBarMenuStyles = makeStyles({
  root: {},
});

export const useNavBarMenuToggleStyles = makeStyles({
  root: {},
});

export const useNavBarMenuItemStyles = makeStyles({
  root: {},
});
